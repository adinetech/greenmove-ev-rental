# Server Configuration
PORT=5050
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/greenmove

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Fare Configuration (in INR)
BASE_FARE=10
PER_MINUTE_CHARGE=2
PER_KM_CHARGE=5

# Carbon Calculation (kg CO2 saved per km)
CARBON_SAVING_PER_KM=0.108

# Vehicle Reservation Timeout (minutes)
RESERVATION_TIMEOUT=5
